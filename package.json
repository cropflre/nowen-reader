{
  "name": "nowen-reader-dev",
  "version": "0.0.0",
  "private": true,
  "description": "Root development launcher for NowenReader",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "go run ./cmd/server",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:setup": "npm --prefix frontend install"
  },
  "engines": {
    "node": ">=20"
  }
}
